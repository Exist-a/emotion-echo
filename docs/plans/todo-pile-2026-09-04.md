---
status: planned
priority: high
owner: TBD
created: 2026-09-04
related-stages:
  - stage-37-fixes-roadmap.md §二 Stage 37-B / 37-C 批次
  - stage-38-system-status.md §三阻断 1/2/3/4/5/6
  - stage-39-nacos-enablement.md §七 未做项
related-decisions:
  - adr-2026-09-doc-drift-registry.md（决策 18）
related-plans:
  - observability-compose-gap.md
  - db-migration-auto-apply.md（2026-09-04 已落地，但本文档列出与 migrations 相关的待办）
---

# Plan — 待办堆栈 2026-09-04（dev 模式尚未真正可用 / 仍未关闭的项）

> **本文件不是新的功能计划，而是把本轮（APISIX 网关 + migrations 自动应用）
> 修复过程中实测暴露、但**未做**的事项全部汇总在一处**。每条都有复现命令、现状
> 证据、修复路径；标注"未解决"以便未来排期筛选。
>
> 与已有 plan 的关系：本文不复述 plan 里写过的内容，只补"plan 没列、本轮实测发现"。

---

## A. dev 模式用户路径阻断（影响"产品能否使用"）

### A1. TTS 语音回复不可用 🔴（产品功能缺失，非路径问题）

- **现状实测**（2026-09-04）：
  ```
  $ curl -X POST http://localhost:19080/api/v1/tts/synthesize \
      -H "Content-Type: application/json" -d '{"text":"hi"}'
  {"error":"Get \"http://emotion-echo-xtts:8003/health\": context deadline exceeded"}
  ```
- **根因链**：XTTS 容器已在 Stage 39 §五被删除（依据是 ai-svc 容器无 `XTTS_BASE_URL` env，
  `aiclient/xtts.go` 空即返 nil）。**删除合理**——但前端 TTS 按钮没同步下线，
  实际上仍是"用户点 TTS 收不到任何东西"。
- **未做原因**：构建 XTTS 镜像受 Stage 36 记录的"pypi CDN 0 字节响应 + Docker Desktop
  内存限制 30+ 分钟"卡死。生产网络跑可能一次成。
- **修复路径（任选一）**：
  1. 生产网络重建 XTTS 镜像：半天到一天
  2. 前端降级到 Web Speech API：几小时（音色能力下降）
  3. 干脆前端下线 TTS 按钮
- **需要决策**：方案 1 vs 2 vs 3

> **🔧 2026-09-04 文档失真就地更正**（决策 18 §4.4 / 登记实例 #12）：
>
> 上面"XTTS 容器已在 Stage 39 §五被删除（依据是 ai-svc 容器无 `XTTS_BASE_URL` env，
> `aiclient/xtts.go` 空即返 nil）"的根因链**两处失真**：
>
> | # | 原断言 | 实测事实 | 证据 |
> |---|---|---|---|
> | 失真 a | "XTTS 容器已在 Stage 39 §五被删除" | `deploy/docker-compose.apps.yml:518` `emotion-echo-xtts` 服务定义**仍在**（含 build context、healthcheck、deploy resources）；顶层 `docker-compose.yml:64-74` 也仍保留 XTTS 服务 | `grep -n "emotion-echo-xtts" deploy/docker-compose.apps.yml deploy/docker-compose.infra.yml` 命中 5 处服务定义 |
> | 失真 b | "`aiclient/xtts.go` 空即返 nil" | `emotion-echo-ai-svc/internal/aiclient/xtts.go` 是 151 行完整实现（`XTTSClient` + `Synthesize` + `SynthesizeToWAV` + `Health`），文件头注释（行 15-21）明写"完整模式：返回 base64 WAV，适合 < 100 字的合成"。**只有当 `BaseURL==""` 时 `NewXTTSClient` 才返 nil**——是构造时降级，不是"空文件" | `wc -l emotion-echo-ai-svc/internal/aiclient/xtts.go` → 151 |
> | 失真 c | "ai-svc 容器无 `XTTS_BASE_URL` env" | `deploy/docker-compose.apps.yml:387-389` 显式注入：`XTTS_BASE_URL: ${XTTS_BASE_URL:-http://emotion-echo-xtts:8003}`（默认指向容器 DNS） | 直接读 apps.yml:387-389 |
>
> **真实根因**：ai-svc 默认 `etc/ai-api.yaml:69-81` 把 `FER/SenseVoice/XTTS.BaseURL` 显式留空，注释明写"启用 AI profile 后 compose 文件会注入容器 DNS 名"。**dev 默认模式即"不调用"**，不是"容器被删除"。
>
> **结论不变**：TTS 在当前 dev 默认配置下仍不可用（503 链路超时）。但修复路径需要重新评估——既然容器定义在、yaml 留空是有意降级，那"重建镜像"不再是唯一选项；用 `docker compose --profile ai up` 启用 AI profile 才是预期开关动作（且该路径在 QUICKSTART.md:101 已记录）。
>
> **修复路径（重评后）**：
> 1. dev 启用 AI profile（一条 compose 命令）→ TTS 立刻可用，**前提是镜像已构建**
> 2. 镜像未构建场景：生产网络 rebuild / 换 Coqui pre-built / 前端降级 Web Speech / 前端下线 TTS 按钮
> 3. 真正的产品决策 = "dev 是默认带 AI profile 还是按需 enable"

### A2. 文件上传后端未实现 🟡（Stage 30 T4.58 占位）

- **现状实测**（2026-09-04）：
  ```
  $ curl -X POST http://localhost:19080/api/v1/uploads/image -H "X-User-Id: 1"
  {"code":1,"message":"uploads not implemented (Stage 31)"}
  HTTP 502
  ```
- **根因**：`emotion-echo-web-bff/internal/handler/upload_handler.go` 头部注释明写
  "Stage 31（引入对象存储）"。**注意**：是 502 而非 404，Stage 38 §三阻断 2 把这条
  记为"路由 404"是误诊，本轮已纠正。
- **前端代码现状**：`emotion-echo-web/app/composables/useFileUpload.ts:42-46`
  调的是**单数** `/upload/image`/`/upload/video`/`/upload/file`，而 BFF 注册的是
  **复数** `/uploads/:kind`（`upload_handler.go:27`）。即使后端实现，路径仍然对不上。
- **修复路径**：先做对象存储选型（MinIO 自建 / 云 OSS），再补后端，**同步修前端路径**。
  两端都得改，是同一笔工作量。
- **需要决策**：MinIO vs 云 OSS（自建无云依赖但要管存储；云省事但引入凭证管理）

> **🔧 2026-09-04 文档失真就地更正**（决策 18 §4.4）：
>
> 上文两处行号需校正（`main.go:232` 实际是 `NewSurveyHandler`，不是 `NewUploadHandler`）：
>
> | 项 | 原断言 | 实测事实 | 证据 |
> |---|---|---|---|
> | 行号 | "`main.go:232` 有 `handler.NewUploadHandler().Register(r)`" | 实际行号是 **`main.go:236`**；`main.go:232` 是 `handler.NewSurveyHandler(s.Assessment).Register(r)` | 直接读 `main.go:230-240` |
> | 前端行号 | "`useFileUpload.ts:42-46`" | 实际行号是 **`useFileUpload.ts:39-48`**（switch 分支结构完整） | 直接读 `useFileUpload.ts:39-48` |
> | 上传后端预期行为 | （隐含假设"路径对就能上传"） | 实际 handler 不论路径对错**一律返 502 "Stage 31 not implemented"**——修前端路径仍会撞 502，必须配合对象存储选型落地 | `upload_handler.go:26-30` |
>
> **结论不变**：A2 是真未做。但修复工作量是"前端改路径 + BFF 真实实现"两步串联，
> 前端单边修没用，必须与对象存储选型同步推进。

### A3. 前端 Nuxt dev server 需要用户手工起 🟡

- **现状实测**（2026-09-04）：`http://localhost:3000/` 301（指向 /login），
  `/login` 200 但内容是 Nuxt 客户端 SPA 壳，**首屏完全靠 JS 在浏览器里渲染**。
  也就是说前端的 HTML 是空的，所有逻辑靠 hydration。curl 看不到任何实质内容。
- **唯一阻断**：用户从仓库 clone 后必须手工 `cd emotion-echo-web && pnpm install && pnpm dev`。
  README 写了这步，但新人未必看到。
- **修复路径**：
  1. 在 QUICKSTART.md 顶部加"5 秒必读"小框，明确"前端是独立进程，要单独起"
  2. 或者起一个 `make dev` / `pnpm dev:all` 同时起前后端
- **预估**：1 小时

---

## B. APISIX / Nacos / 配置中心遗留（架构层面）

### B1. 容器化前端对 BFF 端口的依赖仍存在 🟡（dev 例外，非修复）

- **现状实测**（2026-09-04）：
  ```
  emotion-echo-web-bff  0.0.0.0:8894->8894/tcp
  ```
- **根因**：`deploy/docker-compose.apps.yml:545` 注释
  ```
  # Stage 33 PR-20: BFF 端口保留（dev 调试 / Postman 直连；prod 应仅暴露 APISIX）
  ```
  这是**有意保留的 dev 例外**。本轮我曾误判它"与决策 12 不符"（cf. 决策 18 实例 #1），
  实测后发现是注释已经写明"prod 应仅暴露 APISIX"。
- **风险**：dev 环境下绕过 APISIX 直连 BFF 是可行的。BFF 自身不验签（决策 12
  "BFF 内部鉴权：不做"），只信任 `X-User-Id` header——直连的人塞 `X-User-Id: 1` 就能
  假冒任何用户。dev 监听本机问题不大，prod 部署必须确认 compose 不暴露 8894。
- **修复路径**：
  1. dev 保留现状，prod compose 用独立 profile / 显式不开 ports
  2. 或者在 web-bff 服务上加注释"prod 必须用 ports: []"
- **建议**：写明"prod profile" compose 覆盖文件。

> **🔧 2026-09-04 表述精度补正**（决策 18 §4.4，未达"失真"但易误读）：
>
> 上文"BFF 自身不验签，只信任 `X-User-Id` header"是**简化的近似说法**——
> 实际行为依赖 `BFF_TRUST_APISIX` 环境变量：
>
> | `BFF_TRUST_APISIX` | BFF 行为 | 实际部署 |
> |---|---|---|
> | `true`（默认） | 只信 APISIX 注入的 `X-User-Id`，**不做 JWT 解析** | dev 直连 BFF、prod 经 APISIX |
> | `false` | 走 Authorization JWT 兜底（决策 9 提到的"BFF 透传校验"路径，**当前 JWT 不验签**——审计 P0 S-1） | 本地兼容路径（apps.yml:594-596 注释明写） |
>
> 决策 12 写"信任 APISIX 已验签 + 注入 X-User-Id"对应 `BFF_TRUST_APISIX=true` 路径；
> 决策 9 写"当前 JWT 不验签"对应 `false` 路径。**两条决策适用于不同分支**，不是冲突。
>
> 风险叙述（"直连的人塞 `X-User-Id: 1` 就能假冒任何用户"）只在 `true` 路径下成立；
> `false` 路径即便绕过 APISIX，仍需 Authorization header 才能拿到 user_id。

### B2. Nacos Go SDK 升级阻塞 🟡

- **根因**：Nacos Go SDK v2.3.5 与 Server v2.4.3 long poll 路径不匹配——
  SDK v2.3.x 走老的 `/listener`，Server v2.4.3 走 `/config/listener`，导致配置
  热更新推送无法触发回调（`ListenConfig` 挂上后 web-bff 收不到 callback）。
- **修复路径**：等 Nacos Go SDK 官方发 v2.4 stable tag；或临时降级 Nacos Server 到
  v2.3.x（Stage 39 §四.4 记录过 v2.3.2 Register 返 400 失败）。
- **当前不阻塞**：dev 用作开关即可，启动时 GetConfig 仍正常；热更新是 prod 才需要。

### B3. APISIX 配置中心 seed 与运行期 host 列表 🟢

- **现状**：`charts/emotion-echo/charts/apisix/values.yaml` 里 `nacos.host` 默认
  `emotion-echo-nacos:8848`（dev 单节点）。prod 需要多节点。
- **影响**：仅 prod 范围。

### B4. AI 服务容器（fer / sensevoice / xtts）被删意味着多模态功能不可用 🟡

- **现状**：Stage 39 §五把这三个本地 AI 模型容器都删了（依据是 ai-svc 容器 env 无对应
  BASE_URL env，ai-svc 端空即返 nil）。这意味着**人脸情绪识别、语音转录**这两类
  多模态输入是空跑——前端点对应按钮，要么直接报错，要么上传了但没结果。
- **影响范围**：比 A1 范围更广。A1 只是 TTS 输出端，B4 是**输入端**两个功能都废。
- **修复路径**（与 A1 类似，需决策）：
  1. 重建 FER + SenseVoice 镜像（与 XTTS 同样的构建环境问题）
  2. 把多模态降级为"纯文本情绪"——前端去掉对应入口

> **🔧 2026-09-04 文档失真就地更正**（决策 18 §4.4 / 登记实例 #12）：
>
> 上面 B4 整段**三处严重失真**，与 A1 同根因（详见 A1 更正块）。合并说明：
>
> | # | 原断言 | 实测事实 | 证据 |
> |---|---|---|---|
> | 失真 ① | "Stage 39 §五把这三个本地 AI 模型容器都删了" | `deploy/docker-compose.apps.yml:437/473/518` 三个服务定义**全部仍在**（含 build context、healthcheck、deploy resources），顶层 `docker-compose.yml:47/64/134/135` 也保留 sensevoice / xtts 服务 | `grep -n "emotion-echo-fer\|emotion-echo-sensevoice\|emotion-echo-xtts" deploy/docker-compose.apps.yml` 命中 12 处 |
> | 失真 ② | "ai-svc 端空即返 nil" | `emotion-echo-ai-svc/internal/aiclient/{fer.go,sensevoice.go,xtts.go}` 三文件**全部存在且为完整实现**（行数 130 / 118 / 151，均含 `New*Client` + `Analyze*` + `Health` 方法）。**仅当 `BaseURL==""` 时 `NewXxxClient` 返 nil**——是构造时降级，不是"空文件" | `ls emotion-echo-ai-svc/internal/aiclient/` + `wc -l` 各文件 |
> | 失真 ③ | "前端点对应按钮，要么直接报错，要么上传了但没结果" | 前端 `composables/useFaceEmotion.ts:121` 仍在调 `/face/emotion`，`useTTSPlayer.ts:180` 仍在调 `/tts/stream`——前端入口**未砍**。`useFileUpload.ts:39-48` 也在调上传。| grep 前端 composables |
>
> **真实根因**：ai-svc 默认 `etc/ai-api.yaml:69-81` 把 `FER/SenseVoice/XTTS.BaseURL` 显式留空（注释明写"启用 AI profile 后 compose 文件会注入容器 DNS 名"）。dev 默认配置走"仅文本情绪"降级，**不是"容器被删除"**。
>
> **结论修正**：
>
> | 维度 | 原 B4 描述 | 修正后 |
> |---|---|---|
> | 容器状态 | 已删 | 全部定义在，等 `--profile ai` 启用 |
> | 客户端实现 | 空即返 nil | 完整实现，仅 BaseURL 空时构造降级 |
> | 影响 | 输入端多模态全废 | dev 默认模式下确实不可用，但不是"被删"，是"按需启用" |
> | 修复路径 | "重建 FER + SenseVoice 镜像" / "前端去掉入口" | 1) `docker compose --profile ai up` 启用 AI profile（前提：镜像已构建）；2) 镜像未构建场景同 A1 三选一；3) 真决策 = "dev 是否默认带 AI profile" |
>
> **决策 18 §三 类型 5（自报告失真）新案例**：本条与 A1 同源——作者本人就是探测者，按"看到 dev 默认行为 → 推断为'已删'"得出结论，未 grep `aiclient/` 与 `docker-compose.apps.yml`。补救已在 A1/B4 同做。

---

## C. 文档失真（决策 18 持续登记）

### C1. Stage 38 §三阻断 2/3 误诊 ✅ 已纠正，本轮提交 `049022c`

详见决策 18 §二 实例 #6。修正：路由都挂着，是路径写错或编码方式不一致。

### C2. Stage 38 §四隐患 2 误诊 ✅ 已纠正，本轮提交 `049022c`

详见决策 18 §二 实例 #5。修正：视图能建出来，缺的是它依赖的表所在的迁移。

### C3. Stage 39 §七 "4 个 Go svc unhealthy" 不复现 ✅ 已纠正，本轮提交 `cf1c798`

docker ps 显示六个业务容器全部 healthy。

### C4. **新增** Stage 36-D Bug 2 修复实际从未生效 🆕

- **现状**：第②项 initdb 容错包错语句这件事，从写下的那天起（2026 早期）就没人发现。
  Stage 36-D 报告把这条标为"已修"，但实际它**修了个寂寞**——注释描述的保护对象
  与实际保护对象不一致。`deploy/db/02-create-tables-in-schemas.sql:118-119` 的注释
  本轮已纠正为真实行为。
- **失真原因**："未复跑即记录"——容错写完后没有 `down -v` 验证过空库情况
  （注释里也只写"在 event_id 列不存在时会失败"，没写验证方法）。
- **教训**：决策 18 决策 4.2 "已知问题须带验证日期"正好对症。

### C5. **新增** QUICKSTART.md 与 6 端口命名漂移 🆕

- **现状**（2026-09-04 调查时顺手发现，未展开修正）：
  QUICKSTART.md 第 43 行写"**BFF (唯一入口)** | :8894"——与决策 11/12 矛盾。
  决策 11/12 写得很清楚 BFF 只是 APISIX 的 upstream，唯一对外入口是 APISIX。
  这条是 Stage 30 时代"BFF 取代 APISIX"退场后没回收的措辞。
- **修复路径**：QUICKSTART.md 加一句"**APISIX 是唯一业务入口（决策 11/12）**"，
  并把端口表里的 BFF 一行改为"**BFF (APISIX 后端，dev 调试可直连)**"。

> **🔧 2026-09-04 表述精度补正**（决策 18 §4.4）：
>
> 上文"决策 11/12 写得很清楚 BFF 只是 APISIX 的 upstream，唯一对外入口是 APISIX"
> 不完全准确——决策 9（`decisions.md:127-135`）**仍写 web-bff 是"统一入口"**，
> 决策 9 的架构全景图（`decisions.md:274`）也称 web-bff 为"唯一前端入口"。
> 决策 12（`decisions.md:162-171`）说"宿主机不再直接映射"与全景图共存。
>
> **真实状态**：决策栈 9/11/12 在"BFF 是否唯一入口"上**语义未收口**——
> 决策 9 / 全景图站"是"，决策 12 站"不是（仅容器内）"。QUICKSTART 第 43 行
> 只是承袭决策 9/全景图的多数派措辞，本身没错——错的是决策栈没收口。
>
> 本条 C5 的修复路径需同步做两件事：
> 1. QUICKSTART.md 端口表 BFF 一行加 "(APISIX 后端，dev 调试可直连)"
> 2. `decisions.md` 决策 9 与 12 末尾补"关系说明"段，把端口表的"dev 例外"语义显式化

### C6. **新增** Stage 38 阻断 6 "quick-login 端点从未实现" 🆕

- **现状**：前端 `quickLogin` 函数期望后端有 `/api/v1/auth/quick-login`，
  实际后端无此端点；前端实际走标准 `/api/v1/auth/login` 用 echo/echo123。
  E2E `login-flow.spec.ts` 注释明写"由于 dev 模式…quickLogin 异步调用失败"——
  E2E 从来没真正通过。
- **影响**：用户点"用演示账号快速体验"按钮实际能登录（echo/echo123），但行为/预期
  不一致。**不算阻断**，但属于"文档/测试与代码三方口径不一"的典型例子。

### C7. **新增** Stage 36-FU 报告 16/16 绿但 dashboard 空 — 根因可能也是缺迁移 🆕

- **现状**：Stage 36 报告 4 个 dashboard `chartData.length === 0`。当时归因于 Kafka off
  等多种可能，未结案。
- **本轮新增证据**：smoke §4 `/reports/daily` 实际查 `msg_summary_v` 视图，该视图建
  在 `emotion_echo_chat.messages` 等表上。**全新环境缺此视图**时返 500。**如果 dev
  库历史上长期缺该视图**，dashboard 空的现象与"视图不存在 → SQL 报 column 不存在 →
  兜底为空"完全自洽。
- **待办**：回查 Stage 36 时的 dev 库是否真的缺 `msg_summary_v`（即本轮已落地的
  `analytics/001`）。这若是根因，则 Stage 36 报告的 G4 类问题本质都是 migrations
  缺失类问题。

### C8. **新增** BFF 路由清单无契约测试 🆕

- **现状**：`emotion-echo-web-bff/main.go` `registerRoutes` 函数用 `r.POST(...)` 散
  装注册，**没有任何文件**断言"这些路径的集合是 X"。前端/契约测试只能事后端
  `curl` 验证。Stage 38 §三阻断 2/3 误诊就是这类问题的副作用——没有源生清单，
  curl 404 之后排查"是路由缺失还是路径写错"要读 handler.go。
- **修复路径**：写一个 `_test.go` 用 Go 反射（或直接拉一个全局 list）断言
  `registerRoutes` 注入的路径集合是 `{[/health /metrics /api/v1/auth/:action ...]}`。
- **预估**：1-2 小时（含契约 4/5/6 同步加 seed 端的覆盖）。

> **🔧 2026-09-04 范围扩展**（决策 18 §4.4）：
>
> 路由契约缺陷不止 BFF 单边——前端 `composables/{useFileUpload,useFaceEmotion,useTTSPlayer}.ts`
> 各硬编码一份路径；APISIX `seed.sh` 是第三份路径定义。三方没对齐，是 C6（quick-login）、
> C7（dashboard 空）、A2（upload 单复数）三个待办的共同上游缺陷。
>
> **修复路径（扩展后）**：
> 1. BFF `main.go` 改用 `var routes = []Route{...}` 常量切片 + 遍历注册
> 2. 前端建 `app/lib/apiRoutes.ts` 单点定义，composables 改 import
> 3. APISIX `seed.sh` 改用变量引用统一路径清单（脚本侧生成）
> 4. 加 `tests/integration/test_route_contract.sh` 三方对比，diff 即 fail
>
> **预估**：1.5-2 天（单 BFF 侧 1-2 小时，前端收口 4 小时，APISIX 同步 2 小时，
> 契约测试 4 小时）。

---

## D. 杂项（小、可以批量做）

| # | 项 | 工作量 | 备注 |
|---|---|---|---|
| D1 | 清理 .zcode/、playwright-report/、test-results/ 这几个未跟踪目录 | 5 分钟 | 上游已加进 .gitignore 规则；本地手动删 |
| D2 | 删本地 30+ 条已合并的旧分支（feat/stage-31-pr0X、fix/chart-contract-alignment 等）| 5 分钟 | `git branch --merged main \| grep -v main \| xargs git branch -d` |
| D3 | user_oauth 表零引用 | 决策 18 的失真源头之一；删还是留要由 ADR 决定 | 1 小时 |
| D4 | BFF `isLocked` + `recordFailure` 登录限流写了但无测试 | 半天 | Stage 38 阻断 6 同区域 |
| D5 | chat-svc 表依赖清单 ADR（决策 18 §4.5 标注"未做"）| 半天 |  |
| D6 | Helm chart 与 compose dev 配置全面对齐 | 1 天 | 周期性任务，本轮只动了 APISIX 段 |

---

## E. 与 plan 文档的关系（不重复）

本文件**只列**实测暴露、但还没做的事情。已有的 plan 各自描述"如何做"：

- [observability-compose-gap.md](observability-compose-gap.md) — dev compose 缺 prometheus/grafana/loki/alertmanager/promtail，APISIX SkyWalking endpoint 配错（127.0.0.1:12800），access_log 未挂 volume
- [db-migration-auto-apply.md](db-migration-auto-apply.md) — **本轮已落地**（提交 0d17e85），但仍留有"可重复执行 + 不依赖 initdb.d"的后续题目
- [nacos-enablement-dev.md](nacos-enablement-dev.md) — Stage 39 已落地
- [ai-response-structured.md](ai-response-structured.md) — 前端 marked 已装，分类渲染未做
- [intent-classification-6-types.md](intent-classification-6-types.md) — chat-svc 当前只产 2 类，6 类扩展 backlog
- [file-upload-message-extension.md](file-upload-message-extension.md) — **与 A2 同源**，A2 是后端，本 plan 是前端+消息扩展

---

## F. 复现命令汇总（便于任何时候一键复现当前状态）

```bash
# 1. APISIX 路由契约
bash deploy/apisix/test_seed_nacos.sh

# 2. 数据库迁移契约
bash deploy/db/test_migrations_contract.sh

# 3. 数据契约 smoke
python scripts/smoke_data_layer.py

# 4. 端到端——经 APISIX 真登录
TOK=$(curl -s -X POST http://localhost:19080/api/v1/auth/login \
  -H "Content-Type: application/json" -d '{"username":"echo","password":"echo123"}' \
  | python -c "import sys,json;print(json.load(sys.stdin)['data']['accessToken'])")
for p in users/me conversations surveys; do
  printf "%-20s" "$p"
  curl -s -o /dev/null -w "HTTP %{http_code}\n" \
    "http://localhost:19080/api/v1/$p" -H "Authorization: Bearer $TOK"
done

# 5. 路径不一致（已修但仍可验证）
grep -n "/upload/\|/uploads/\|multimodal/face\|/api/v1/ai/tts" \
  emotion-echo-web/app/composables/useFileUpload.ts \
  emotion-echo-web/app/pages/chat/conversation/new.vue

# 6. 真实环境是否从零可用
docker compose -f deploy/docker-compose.infra.yml -f deploy/docker-compose.apps.yml down -v
docker compose -f deploy/docker-compose.infra.yml -f deploy/docker-compose.apps.yml up -d
sleep 60
python scripts/smoke_data_layer.py
# 期望: 10/10 PASS
```

---

## G. 排期建议（仅建议，未排期）

按"用户能立即用 / 修起来快"由高到低：

1. **A3** 起前端说明（1 小时）—— 已完工等用户执行
2. **C5** QUICKSTART.md 入口表述修正（30 分钟）
3. **C8** BFF 路由清单契约测试（1-2 小时，配合第三步的路径修复一起做）
4. **A1 + B4** TTS / 多模态 输入输出方案（需要用户拍板，半天到一天）
5. **A2** 文件上传（需要选对象存储，1-2 天）
6. **B1** dev/prod 端口分离（半天，写明 prod compose override）
7. **observability-compose-gap** 独立推进（不在本文档管）
8. **C7** 回查 Stage 36 dashboard 空（半天）
9. **C6** quick-login 端点（1-2 小时，删前端按钮或真实现）
10. **D3-D6** 杂项批处理
