# XTTS-v2 minimal requirements (Stage 25-B)
# 不锁 numpy 版本，让 Coqui TTS 自己解析（避免冲突）
# Coqui TTS 0.22 + Python 3.10 会拉 numpy>=1.24,<3.0（已自动满足）

torch>=2.0.0,<3.0.0
torchaudio
modelscope
TTS>=0.21.0,<1.0.0   # Coqui TTS 开源版（1.0+ 后卖给 HF 停止维护）
fastapi>=0.100.0
uvicorn>=0.23.2
pydantic>=2.0
prometheus-client>=0.19.0
python-json-logger>=2.0.7
cython>=3.0.0  # Cython 3+ 兼容 Python 3.10
