# XTTS-v2 minimal requirements (Stage 25-B)
# 国内 pip mirror 加速，避免 TTS 安装时拉 spacy[ja] 等大包失败
# Coqui TTS 自身会拉 numpy / numba / cython 等依赖

torch>=2.0.0,<3.0.0  # 锁大版本，避免不兼容
torchaudio
modelscope
TTS>=0.21.0,<1.0.0   # 锁 TTS < 1.0（开源版，Coqui 卖给 HF 后停止维护）
fastapi>=0.100.0
uvicorn>=0.23.2
pydantic>=2.0
numpy>=1.24.3,<3.0.0  # 强制新 numpy（兼容 Python 3.10/3.11）
prometheus-client>=0.19.0
python-json-logger>=2.0.7
# 显式锁 Cython，避免 TTS 拉老版本（Py3.10 编译问题）
cython>=3.0.0