image:
  repository: emotion-echo/web
  tag: v0.1.0
  pullPolicy: IfNotPresent
replicas: 1
service:
  port: 3000
resources:
  requests: {cpu: 100m, memory: 128Mi}
  limits: {cpu: 500m, memory: 512Mi}
config:
  # Stage 30: 前端唯一入口改为 emotion-echo-web-bff（BFF 替代 APISIX 网关）
  apiBaseUrl: "http://web-bff.ee-app:8894/api/v1"
  ttsBaseUrl: "http://ai-svc.ee-app:8891/api/v1"
  llmModel: local